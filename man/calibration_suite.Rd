% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eval.R
\name{calibration_suite}
\alias{calibration_suite}
\title{Calibration suite (the manuscript headline)}
\usage{
calibration_suite(prob, y, n_boot = 1000L, seed = 20260508L)
}
\arguments{
\item{prob}{Numeric vector of predicted probabilities.}

\item{y}{Outcome (0/1 integer).}

\item{n_boot}{Bootstrap replicates; default 1000.}

\item{seed}{Integer seed; default 20260508.}
}
\value{
A list with elements \code{slope}, \code{intercept}, \code{ici}, \code{cit_large}
(each named \code{c(estimate, lower, upper)}), \code{curve} (a \code{data.frame}
for plotting), and \code{n}, \code{n_events}.
}
\description{
Computes calibration slope, intercept, integrated calibration index
(ICI), calibration-in-the-large, and a smoothed calibration curve
(\code{loess}). Bootstrap percentile CIs (1,000 reps default) on every
point estimate.
}
\details{
Definitions:
\itemize{
\item \strong{Calibration slope}: logistic regression of \code{y ~ logit(prob)},
slope coefficient. Ideal = 1; <1 indicates over-fitting.
\item \strong{Calibration intercept}: logistic regression of
\code{y ~ offset(logit(prob))}, intercept. Ideal = 0; >0 indicates
systematic under-prediction.
\item \strong{Calibration-in-the-large}: \code{log(prev_obs / prev_pred)}.
Ideal = 0.
\item \strong{ICI}: mean absolute difference between smoothed observed and
predicted probabilities.
}
}
