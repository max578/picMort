% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{fit_xgboost}
\alias{fit_xgboost}
\title{Fit XGBoost classifier}
\usage{
fit_xgboost(features, train_idx, grid = NULL, nfolds = 5L, seed = 20260508L)
}
\arguments{
\item{features}{Output of \code{\link[=build_features]{build_features()}}.}

\item{train_idx}{Integer vector of training-row indices.}

\item{grid}{\code{data.frame} of hyperparameter combinations. If \code{NULL}, uses
the canonical 4-row grid documented in
\code{vignettes/paper1_baseline.Rmd}.}

\item{nfolds}{Inner CV folds; default 5.}

\item{seed}{Integer seed; default 20260508.}
}
\value{
A list with \code{model}, \code{prep}, \code{predictors}, \code{best_params},
\code{best_nrounds}, \code{cv_log}, \code{scale_pos_weight}, and \code{type = "xgboost"}.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Inner CV over a small principled grid. Class imbalance handled via
\code{scale_pos_weight = n_neg / n_pos}. Returns the best parameter set
and the corresponding fitted model.
}
\examples{
\dontrun{
paths    <- pic_paths()
cohort   <- build_cohort(paths, min_los_hours = 24L, verbose = FALSE)
features <- build_features(cohort, paths, window_hours = 24L)
split    <- make_train_test_split(features)
fit      <- fit_xgboost(features, split$train_idx)
fit$best_nrounds
}
}
