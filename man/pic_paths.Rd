% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io_paths.R
\name{pic_paths}
\alias{pic_paths}
\title{Resolve PIC v1.1.0 source paths}
\usage{
pic_paths(root = NULL, check = TRUE)
}
\arguments{
\item{root}{Path to the directory containing \verb{data_links/}. Defaults
to the project root (two levels above the package directory when
loaded via \code{devtools::load_all()}).}

\item{check}{Logical. If \code{TRUE} (default), verifies that every
referenced CSV exists and is readable.}
}
\value{
Named list with elements \code{admissions}, \code{patients}, \code{icustays},
\code{chartevents}, \code{labevents}, \code{diagnoses_icd}, \code{d_items},
\code{d_icd_diagnoses}, \code{d_labitems}, \code{prescriptions}, \code{inputevents},
\code{outputevents}, \code{microbiologyevents}, \code{surgery_vital_signs},
\code{or_exam_reports}, \code{emr_symptoms}.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Returns a named list of canonical paths to PIC CSVs, resolved through
the project-local \verb{data_links/pic_v110/} symlink. The package never
embeds the Box-synced source path; the symlink is the single point of
indirection.
}
\examples{
# Requires the registered PIC v1.1.0 source linked at
# `<project root>/data_links/pic_v110/`. Run `check = FALSE` to inspect
# the expected file names without touching disk.
\dontrun{
paths <- pic_paths()
names(paths)
}

# File-name discovery without verifying existence
tmp <- tempfile(); dir.create(file.path(tmp, "data_links", "pic_v110"),
                              recursive = TRUE)
paths <- pic_paths(root = tmp, check = FALSE)
names(paths)
unlink(tmp, recursive = TRUE)
}
