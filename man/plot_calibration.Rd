% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eval.R
\name{plot_calibration}
\alias{plot_calibration}
\title{Render the headline calibration plot}
\usage{
plot_calibration(calib_list)
}
\arguments{
\item{calib_list}{Named list; each element is the output of
\code{\link[=calibration_suite]{calibration_suite()}} for one model.}
}
\value{
A \code{ggplot} object.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Smoothed loess calibration curves per model with the ideal
diagonal. Returns a \code{ggplot} object; save via \code{ggplot2::ggsave()}.
}
\examples{
if (requireNamespace("ggplot2", quietly = TRUE)) {
  set.seed(20260517L)
  n <- 200L
  prob <- stats::plogis(stats::rnorm(n, -2, 1))
  y    <- stats::rbinom(n, 1L, prob)
  cal  <- calibration_suite(prob, y, n_boot = 25L)
  p    <- plot_calibration(list(model_a = cal))
  class(p)
}
}
