% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eval.R
\name{plot_decision_curve}
\alias{plot_decision_curve}
\title{Render the headline decision-curve plot}
\usage{
plot_decision_curve(dca)
}
\arguments{
\item{dca}{A \code{data.table} from \code{\link[=decision_curve]{decision_curve()}} (with \code{plot_grid = TRUE}).}
}
\value{
A \code{ggplot} object.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Net benefit vs threshold across models, with treat-all and
treat-none references.
}
\examples{
if (requireNamespace("ggplot2", quietly = TRUE)) {
  set.seed(20260517L)
  n <- 200L
  p_a <- stats::plogis(stats::rnorm(n, -2, 1))
  y   <- stats::rbinom(n, 1L, p_a)
  dca <- decision_curve(list(model_a = p_a), y, plot_grid = TRUE)
  p   <- plot_decision_curve(dca)
  class(p)
}
}
