% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{predict_mortality}
\alias{predict_mortality}
\title{Predict mortality probabilities (unified interface)}
\usage{
predict_mortality(model, new_data)
}
\arguments{
\item{model}{Fit object from one of the \verb{fit_*()} functions, OR a
PIM3 \code{data.table} from \code{\link[=compute_pim3]{compute_pim3()}}.}

\item{new_data}{New \code{features$x}-style \code{data.table} (with
\code{icustay_id} and the same columns as the training features).}
}
\value{
A \code{data.table} aligned to \code{nrow(new_data)}.
}
\description{
Routes to the appropriate predict implementation based on
\code{model$type}. Returns a \code{data.table} with columns:
\itemize{
\item \code{prob_raw}         -- model output (probability of in-hospital mortality)
\item \code{prob_calibrated}  -- post-hoc calibrated (only XGBoost; same as raw otherwise)
\item \code{prob_lower}, \code{prob_upper} -- 95 \% credible interval (Bayesian only)
}
}
